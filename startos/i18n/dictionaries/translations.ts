/**
 * Empty until a speaker of each language fills it in. The SDK falls back to the default
 * dictionary for any locale it does not find here, so shipping this empty is correct rather
 * than merely tolerable.
 */
export default {} as const
